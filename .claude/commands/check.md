---
description: Run analyze + tests and report only what is broken
---
Run `flutter analyze`, then `flutter test`.

Report ONLY failures and warnings, grouped by file, one line of cause each.
If everything passes, say so in a single line and stop.

Do not fix anything unless I ask you to.
